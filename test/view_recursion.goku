# Simple factorial for viewing generated C code
factorial = (n) -> {
    if (n == 0) then {
        return 1
    } else {
        return n * factorial(n - 1)
    }
}

# Just one test to keep it simple
assert (factorial(3) == 6)
